[package]
name = "pretraining-goal-conditioned-world"
version = "0.1.0"
edition = "2021"

[lib]
name = "pretraining_goal_conditioned_world"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
pretraining-world = { path = "../world" }
