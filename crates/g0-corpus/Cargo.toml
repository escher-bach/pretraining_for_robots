[package]
name = "pretraining-g0-corpus"
version = "0.1.0"
edition = "2021"

[dependencies]
pretraining-g0-contract = { path = "../g0-contract" }
pretraining-g0-render = { path = "../g0-render" }
pretraining-term-compiler = { path = "../term-compiler" }
pretraining-world = { path = "../world" }
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
